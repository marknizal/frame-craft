export interface SectionProps {
  $bg?: string;
}

export interface ImageProps {
  $fit?: "cover" | "contain" | "fill" | "none" | "scale-down";
  $ratio?: string;
  $radius?: string;
  $width?: string;
}
